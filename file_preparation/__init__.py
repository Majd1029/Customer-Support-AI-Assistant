# file_preparation — RAG pipeline: chunking, embedding, indexing, retrieval,
# generation, evaluation, memory, intent, escalation, feedback.
# Importable as `file_preparation` after `pip install -e .`
