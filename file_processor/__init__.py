# file_processor — document extraction engine
# This package is importable as `file_processor` after `pip install -e .`
# Within-package sibling imports (e.g. `from parser import`) continue to work
# because api.py adds file_processor/ to sys.path at startup.
